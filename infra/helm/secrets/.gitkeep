# .gitkeep — placeholder to keep this directory in Git.
# Do NOT commit plaintext secrets here.
#
# Recommended workflow:
# - Store Kubernetes secrets as SOPS-encrypted YAML (e.g. *.enc.yaml).
# - Decrypt only in CI/CD or locally when applying:
#     sops -d infra/helm/secrets/app.enc.yaml | kubectl apply -f -
# - Add patterns to .gitignore to prevent accidental plaintext commits.
#
# Suggested files (encrypted):
# - app.enc.yaml                # app chart Secret manifests
# - postgres.enc.yaml           # DB credentials
# - redis.enc.yaml              # Redis passwords
# - minio.enc.yaml              # MinIO root creds / app keys
# - otel-collector.enc.yaml     # exporters’ auth tokens
# - external-integrations.enc.yaml  # 3rd-party API keys
#
# Keep this file committed; it ensures the directory exists in fresh clones.