
# Run commitlint only if it's installed; otherwise don't block commits.
if command -v pnpm >/dev/null 2>&1 && pnpm -v >/dev/null 2>&1; then
  if pnpm dlx --package=@commitlint/cli@20 commitlint --version >/dev/null 2>&1; then
    pnpm dlx @commitlint/cli@20 --edit "$1" || true
  fi
fi
