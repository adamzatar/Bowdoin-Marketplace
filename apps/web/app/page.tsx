// apps/web/app/page.tsx
// Minimal home page so the root route renders.
// If your app has a real landing, replace this file or redirect accordingly.

export default function Home() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Bowdoin Marketplace</h1>
      <p>Welcome! App Router is configured and building correctly.</p>
    </main>
  );
}